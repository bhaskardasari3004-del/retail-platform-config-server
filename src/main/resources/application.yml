server:
  port: 8888

spring:
  application:
    name: retail-platform-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/bhaskardasari3004-del/retail-platform-config-repo.git
          default-label: Release
          clone-on-start: true

management:
  endpoints:
    web:
      exposure:
        include: "*"